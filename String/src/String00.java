// 변수 메소드 많이 알면 좋음
public class String00 {

	public static void main(String[] args) {
	
	// if(Str.length()>0) 문자열이 존재하냐 안하냐 확인할때 쓰임 0보다 크면 존재 0과 같으면 없음
		// str.startsWith("A"); A라는 글자를 앞에서부터 찾음
		// str.endsWith("A"); 
		// str.indexof("Java") 글자가 처음 나오는 위치를 출력 , 앞에서 부터 찾음
		// str.lastIndexof(".") 글자 위치 찾는다, 뒤에서 부터 찾음 (확장자 찾을때)
		// "Java를 공부 중..  
		// String str2 = str1.replace("Java","자바"); //특수문자를 공백으로 바꿔줌
		// str1.substring(indexof("Java"),4);
		// String str2[] = str1.split(",") 문자를 쪼갤때 사용
		// cotains 위치를 찾고 존재하는지 확인  
	}

}

// 맵에있는 데이터 List 출력